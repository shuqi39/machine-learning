# machine-learning
我学习机器学习的足迹

7.29：机器学习的主要模式分为监督学习和无监督学习，监督学习的主流算法有线性回归和逻辑回归等，无监督学习有k-means聚类，pca等，监督学习指的是既有原始数据x，又有带标签的输出y，目的是预测其他数据x的输出或映射y，具有正确性，无监督学习指的是不带有标签输出的y，只有原始数据x，目的是找出原始数据中的规律，共同点或者一些有意思的东西，正确性是相对的，举个例子，短视频算法中的猜你喜欢是前者，而相似推荐则是后者  
顺便学了一下jupyter

7.30：今天系统学习了线性回归，还是初高中的y = w * x + b,但是要看代价j需要^y和w, b画在一张图需要画三维图，方便看j可以引入等高线概念把相同的j给切成平面图，图形复杂则可使用梯度下降算法，就是贪心算法的梯度方向运用

7.31：梯度下降公式：ω = ω - α * dJ(w, b) / dw  
β = β - α * dJ(w, b) / db  
j是最小化代价函数  
α是学习率，就是梯度的步长，太小会导致学习效率太慢，太大的话可能无法收敛  
到达极小值后，偏导数为0，ω会 == ω  
在接近极小值的过程中，步长会越来越小，因为图像越来越缓，偏导数越来越小

8.1：xj表示第j个样本的数据  
->x^(i)表示第i组样本的数据，用列表表示
->x^(i)j表示第i组样本的第j个数据  
python中常用numpy模块引入线性代数  
w = np.array([1.0], [2.0], [3.0])建立数组
np.dot(w, x)或w.dot(x)或干脆w@x为点乘w, x两个数组，因为numpy重载了@作为矩阵乘号  
向量化数据是因为可以调用GPU(图形处理器)进行高效的矩阵运算，以空间换时间  
多重线性回归的梯度下降公式：  
wn = wn - α∑(i =1 to m)(f->w, b(->x^(i) - y^(i))) * xn^(i) / m  
b = b - α∑(i =1 to m)(f->w, b(->x^(i) - y^(i))) / m  
正规方程：通过高级线代库一次性求解出w, b,无需迭代  
但是它不能推广到其他算法，只适用于多重线性回归算法，而且特征值很多时，其运行速度很慢  
特征缩放：一组样本中不同数据对输出结果的影响可能不一样，比如房子价格中房子大小以及是否有厕所两个数据，特征缩放的意义在于将这些数据归一化，都缩放成0到1，以排除这些因素对特征值和最小化代价函数的影响(本质就是这些数据对应的学习率原来不一样，但将他们缩放成一样)

8.2：缩放特征值的处理就是把所有数据都除以最大数据的值，保证归一化后的最大值为1  
均值归一化则是先计算出数据的平均值u，然后遵循公式x = (x - u) / (xmax - xmin)  
Z分数归一化则是先计算出标准差ς，然后x = (x - u) / ς   
原则上需要将数据缩放到接近+-1的范围，指数级太大或者太小效果都不好  
学习曲线：横坐标为参数的迭代次数，纵坐标为j的值，正常来说j应该是斜率越来越小地下降的，但如果上升了，说明α太大或者代码出现错误，当曲线趋近一条横线时，就是梯度下降收敛了   
当出现了j上升的情况，可以选取一个非常小的α，进行测试，看看是否还会上升，如果不会了说明是α太大了，仍然会的话则是代码有问题  
自动收敛测试：另一种判断迭代次数的方法，选取一个e(比如0.001)，如果j经过一次迭代后减少值<=e，则判断梯度下降收敛，但选择一个合适的e通常比较困难

8.3：逻辑回归实际用于分类  
sg函数：g(z) = 1 / (1+e^-z)  
z = ->w * ->x + b  
g(z)>=0.5判断为yes(正例，1)，否则为no(负例，0)  
决策边界，z = 0，z>0为yes，<0为no，将逻辑回归转化为线性回归(也可能得到圆，椭圆，曲线等函数图像，看这里的->x有几项，幂有几次)  
逻辑回归的成本函数：loss函数：单个样本中预测值与真实标签的差异  
L(f, y^(i)) = -logf (if y^(i) = 1)  
 = -log(1-f) (if y^(i) = 0)  
简化焚诀版：L(f, y^(i)) = -y^(i) * logf - (1-y^(i)) * log(1-f)  
而逻辑回归中的成本函数则是所有样本损失函数的均值或总和(通常是均值)

8.4：欠拟合 = 具有高偏差  
泛化：模型在训练集之外的数据表现  
过拟合：使用幂次很高的x泰勒展开拟合训练集，虽然对训练集的拟合效果很好，但曲线可能波动强烈，对训练集之外的数据泛化效果可能很差 = 具有高方差  
解决过拟合：1.增大训练数据集  
2.使用更少的特征--选择合适的特征集也称为特征选择  
3.正则化--减小参数的大小，通常为w(特别是幂次高的特征值的系数)，正则化b没什么效果，这一过程也叫惩罚某个w，但参数过多时我们不知道惩罚哪些参数比较好，通常的做法是惩罚所有w：引入正则化参数λ，在J公式中加入λ / 2m * ∑wj^2(这叫正则化项)，λ越大，正则化程度越大，f(x)趋近b，成了一条水平线，变成欠拟合，越小(趋近0)，正则化程度越小，依旧是过拟合  
正则化相对于2，是减小高次特征值的影响，而不是2的置零完全去除，所以更温和一些

8.5：神经网络就是深度学习的前身，二者非常相似，早期是模拟生物神经网络的软件，也叫人工神经网络，现在的深度学习和早期的思路相差还是比较大的，现在的神经模型受困于神经学的对大脑理解的局限，是非常简化的，但也足够构造出强大的深度学习网络  
一个神经元就是一个逻辑回归单元，神经网络通过上一层输入数据输出的新值称为激活值，第一层叫输入层，中间层称为隐藏层，最终层叫输出层，输出的通常是一个概率，每层的输入值或者激活值都组成一个向量  
神经网络本质上是可以学习自己特征的逻辑回归  
隐藏层的多少，每层神经元的数量是神经网络架构的问题

.6：输入层一般叫做第0层，简标为[0]，后面是第1层，以此类推
a上标[i]指的是第i层的输出下标表示是这层的第几个神经元
举例：a上标[3]下标2＝w上标[3]下标2点乘a上标[2]＋b上标[3]下标2
Python中的numpy数组在tensorflow中常被转化成张量，一种用于大数据计算的特殊向量

8.12：matmul是NumPy的矩阵乘法方式，上面也说过，z = np.matmul(AT, W)就是AT @ W  
A.T 转置矩阵  
epochs 是表示操作者可能希望运行学习算法的步数

8.13：输入：VGX  
np.exp(z):e^z  
BinaryCrossentropy():二元交叉熵函数--就是上面写的loss函数，我们用的是这个，实际上loss函数还有其他的  
Sequential:是一个容器或类。它是一个“管道”，让你按顺序一层一层地搭建神经网络，数据从输入进去后，会像流水一样依次流过每一层，最后输出结果  
Dense:全连接层，Dense 做的事情就是小学数学的“加权求和 + 加偏置”，公式是：输出 = 输入 × 权重 + 偏置  
其他激活函数：比如ReLU:g(z) = max(0,z)--叫修正线性单元，甚至g(z) = z也是  
ReLU计算更简单，而且图像只有一处平坦，所以使用它为激活函数会使神经网络学习快一些  
二分类问题用sigmoid函数，Y可正可负用线性激活，Y非负用ReLU，隐藏层请默认用ReLU，不要用线性激活函数  
ReLU代码里写的是relu，线性是linear  
只使用线性激活无法拟合，和特征工程无异，不如使用线性回归模型

8.14：softmax回归算法，适用于多分类场景  
aj = e^zj / (k =1 to n)e^zk  
loss = -logaj if y = j  
使用softmax的层叫softmax层，输出叫softmax输出  
这个函数叫稀疏分类交叉熵损失函数SparseCategoricalCrossentropy  
尽量减少中间变量的使用，因为在TensorFlow中这样可以自动找舍入误差小的计算方式计算  
多输出分类：1.视为多个独立机器学习问题  
2.1个神经学习问题解决  
多类分类与多输出分类的区别在于多类分类的标签也有很多个，但最终输出只有一个，而多输出分类则是1个或多个  
Adam(自适应矩估计)算法，自动调节学习率的算法，且它为每个参数单独设置学习率

8.15：卷积层：每个神经元只能查看部分像素，而不能查看全部，优势是速度更快而且需要的训练数据更少，可以减少过拟合的风险，使用卷积层的神经网络叫卷积神经网络  
sympy库可以计算导数  
symbols创建符号变量，.diff(被导项，导项)，.subs(被替换项, 替换项)  
计算图就是类似将计算过程列出来的流程图  
反向传播就是不停的求导，相比于正向传播的N*P步，只需要N+P步，遍历N个主节点，每次修改一个参数，共P次P个分支

8.16：将训练集分为训练集（0.7）和测试集（0.3）两部分，是一种评估模型训练效果的方法
进阶的分割方式可以分成训练集（0.6）、测试集（0.2）和交叉验证集（或者叫开发集）（0.2），交叉验证集是通过每个数据选取不同阶数的X来尽量排除训练结果是否只是偶然在这个训练集上选这个最优阶数达到了最好的效果
Jcv就是交叉验证误差
所以判断一个模型效果怎么样，可以看三个，一是基础表现，二是训练误差，即J train，三是Jcv

8.17：kernel_regularizer = L2(0.01)就是设定正则化参数λ=0.01  
只要适当正则化，选择较大的神经网络不会有过拟合等什么坏处，最多就是训练时间久一点，而且只要训练集不要大的离谱，就不会有高偏差的问题  
机器学习开发的迭代循环：1.选取系统整体架构(什么模型，数据，参数)  
2.实施诊断(查看偏差、方差、错误分析)
3.做出决定，是扩大神经网络，还是改变正则化参数、添加更多数据还是增加或者减少特征  
错误分析就是手动分析或抽取分析算法判断错误的样本，错误分析的局限性在于如果手动也难以判断样本的错误点，效果就没那么理想了

8.18：迁移学习：只有最后的输出层重新设置，其他的都照搬过来  
混淆矩阵：2x2的四个格子，上面两个是实际标签，左边两个是预测标签，都是1为真阳性，都是0为真阴性，实际0预测1为假阳性，实际1预测0为假阴性  
精确度(Precision)：真阳性/阳性--针对错报  
召回率(Recall)：真阳性/(真阳性+假阴性)--针对漏报

8.20：F1：2PR/(P+R)，在难以通过肉眼取舍PR评判算法优劣时可以使用这个指标判断  
决策树学习算法就是从所有可能的决策树中挑选一个在训练集中表现良好且泛化能力好的树，本质上就是很多if-else  
1.决策树每个节点的选择和选择的顺序都会影响算法最终性能  
2.什么时候停止呢？一是当一个叶子节点100%或0%为某标签；二是手动设置深度；三是下一个节点降低不了纯度或收益太低，可以减小过拟合风险；四是节点中的例子数量低于某个阈值  
熵：测量样本不纯度的函数：对数据集中某标签占总数的比例p1:H(p1) = -p1log2 p1 - (1-p1)log2 (1-p1)  其实就是二元交叉熵  
熵的减少成为信息增益：衡量树的分割获得的熵减少量：H(0.5)-(左子树样本量/总样本量wl*H(p1l)+右子树样本量/总样本量wr*H(p1r))  这里的0.5其实是根节点的熵p1root  结果越大越好

8.22：决策树有用到递归  
决策树中的特征值拓展到每个特征有K种，就用k个二元特征表示，这些二元特征值只有一个取1，所以叫做独热编码  
回归树在选择不同特征值进行分割后，再挑已经分割的子树内部，选一个另外的特征值计算方差，然后选择使方差减少大的方法，这样可以使样本更集中，预测效果更好  
单一决策树对数据的微小变化非常敏感,解决方法就是构建许多决策树，称之为树集成，最后让树集成对新样本分类，并让它们投票决定最终的预测结果

8.23：随机森林算法，通过从总训练集中随机抽样获得的各个子训练集构成的生成树构成的森林，选择的特征值也不是全选，一般选择K等于根号n，n为总特征值数量  
XG Boost 算法，引入刻意训练的思想，提高上一颗决策树的错误样本在下一棵树放回抽样中抽到的可能性  
决策树通常在表格类型的训练集上表现良好，决策树训练较快，而且可以回溯